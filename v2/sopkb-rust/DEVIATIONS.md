# Deviations from Python behavior

Per PORT_PLAN.md §6.0: every `§5` row marked FIX, and every decision in `docs/port/DECISIONS.md` that changes
observable behavior, must have (a) a named test proving the new behavior and (b) an entry below. A FIX with no
entry here is indistinguishable from a bug.

| ID | Change | Fixture case / test | Phase |
| --- | --- | --- | --- |
| P-V5 | `validate_bundle` no longer crashes (uncaught `TypeError`/`AttributeError`, exit 1, zero reports written) on the five malformed-state inputs (null `manifest.sources`, null item `confidence`, `items.json` as an object, `inventory.json` as an array, string-valued `warnings` entries). It now completes normally and always writes `reports/validation.json`/`.md`, treating each malformed shape as either a reportable validation error/warning or a silently-tolerated absence (see the per-case comments in `crates/sopkb-review/tests/phase3_malformed_v5_fix.rs` for exactly which). Confirmed against the real fixture corpus that Python's current behavior for all five is a crash with no output at all (`fixtures/cases/malformed-*/expected-python/_transcript.txt`), so there is no byte-for-byte Python output to diff against here. | `v2/sopkb-rust/fixtures/cases/malformed-{null-manifest-sources,null-confidence,items-object,inventory-array,warnings-strings}` via `crates/sopkb-review/tests/phase3_malformed_v5_fix.rs` (5 tests) | Phase 3 |
| P-A35 | `agent_chat.json`'s write-side cap is no longer hard-coded at 25 in the persistence function itself: `sopkb_agent::append_agent_chat_entry` still caps at `DEFAULT_TRANSCRIPT_CAP = 25` by default (so default, unconfigured behavior is byte-identical to Python, including the reference-fixture differential test), but `sopkb_agent::append_agent_chat_entry_capped(bundle_dir, response, cap)` exposes the cap as a parameter so a future caller (Settings screen, Phase 9 Tauri command, etc.) can choose a larger or effectively-unlimited value instead of silently destroying a user's chat history on every write, per PORT_PLAN.md P-A35's explicit recommendation ("raising the cap or making it configurable... silently destroying a user's chat history is hard to defend"). The single-shot model itself is unchanged and PRESERVED: the transcript is still never fed back into the model as conversation history. No caller in this phase actually passes a non-default cap, so no currently-observable behavior changes — this is scaffolding for a later phase to use. | `crates/sopkb-agent/src/transcript.rs::tests::custom_cap_can_exceed_25` (proves a raised cap keeps all 30 of 30 appended entries, where the default 25-cap would drop the oldest 5); `crates/sopkb-agent/src/transcript.rs::tests::default_cap_keeps_last_25_oldest_first` (proves the default still matches Python exactly) | Phase 8 |
| P-X8 | `write_section_docs`'s fallback for a section whose `source_id` isn't in the inventory now uses the same two-key `{id: source_id, title: source_id}` fallback the knowledge/evidence/rule/relation writers already use, instead of Python's one-key `{"title": source_id}` fallback (no `id` key), which crashes `source_doc_path(source)` with a `KeyError` and aborts the whole sync. This port never crashes on that condition; it degrades gracefully like the other four writers. | `v2/sopkb-rust/crates/sopkb-export/src/okf_docs.rs::source_or_fallback` (used by all five writers uniformly); `crates/sopkb-export/tests/phase5_gotcha_fixes.rs::section_with_unknown_source_id_does_not_crash_sync` | Phase 5 |
| P-X9 | `group_by` (backing `sections_by_source`/`items_by_source`/`items_by_section`) now silently skips a section/item that lacks `source_id`/`section_id` instead of Python's direct-index `KeyError`, which aborts the entire sync -- even though `validate_bundle` treats the identical condition as a mere reported error and continues. A malformed record is now just missing from that one grouping rather than taking down the whole export. No "recorded warning" surfaces this (recommended by PORT_PLAN.md §5, but there is no existing warnings-plumbing in `sync_okf_bundle`'s return shape to carry one without adding new API surface beyond this phase's scope). | `v2/sopkb-rust/crates/sopkb-export/src/sync.rs::group_by`; `crates/sopkb-export/tests/phase5_gotcha_fixes.rs::item_missing_section_id_does_not_crash_sync` | Phase 5 |
| P-G5 | `export_rdf`'s Turtle output now emits each distinct `a sopkb:Concept` / `a sopkb:RelationObject` block at most once (keyed by local name, deduped across items), instead of Python's one-block-per-item repetition (valid but redundant RDF -- two items sharing a subject produce two identical `Concept` blocks in the original). | `v2/sopkb-rust/crates/sopkb-export/src/rdf.rs::NameRegistry` + `rdf.rs` unit tests; `crates/sopkb-export/tests/phase5_export_bundle.rs::export_rdf_preserves_relation_and_rule_facts_while_deduping` (proves the same knowledgePiece/reviewStatus facts survive with fewer total Concept blocks, against the real `reference` fixture's recorded `triples.ttl`) | Phase 5 |
| P-G6 | The Turtle `sopkb:RelationObject` IRI is now disambiguated when the 64-char slug truncation would otherwise collide: two items whose `object.id` truncates to the same 64-char slug but carry different `label`/`text` now get distinct local names (`<base>_<8-hex-char FNV-1a hash of label+text>` for the second and later distinct pair), instead of Python's silent collision (two different `rdfs:label`/`sopkb:objectText` pairs asserted on the same IRI -- contradictory triples). Deliberately Turtle-only, per PORT_PLAN.md §5 P-G6's own recommendation: `object.id` in relation *documents* (`relations/kr-*.md`) is untouched, so the two representations now deliberately differ. | `v2/sopkb-rust/crates/sopkb-export/src/rdf.rs::name_registry_reuses_identical_reassigns_distinct` | Phase 5 |
| P-G7 | `escape_ttl` now also escapes `\r` as `\r` and `\t` as `\t` (in addition to Python's existing `\\`->`\\\\`, `"`->`\"`, `\n`->space), instead of leaving them raw inside a quoted Turtle literal -- which is technically invalid Turtle for CRLF-derived source text. | `v2/sopkb-rust/crates/sopkb-export/src/rdf.rs::escape_ttl_escapes_backslash_quote_newline_cr_tab`; `crates/sopkb-export/tests/phase5_export_bundle.rs::export_rdf_escapes_cr_and_tab_p_g7` | Phase 5 |
| P-G12 | `export_rdf` now skips a decision rule with no `obligation.fact` (legal for an authored rule with no `obligation` at all) instead of Python's direct-index `rule["obligation"]["fact"]`, which raises `KeyError` and crashes the entire RDF export on otherwise-legal upstream data. | `v2/sopkb-rust/crates/sopkb-export/src/rdf.rs::export_rdf` (the `let Some(obligation_fact) = ... else { continue }` guard); `crates/sopkb-export/tests/phase5_gotcha_fixes.rs::decision_rule_with_no_obligation_is_skipped_not_crashed` | Phase 5 |
| P-W1 | **New per-bundle write lock**, not present in Python at all: every review action (`approve`/`reject`/`defer`/`comment`/`edit`) now runs its entire read-`items.json`-modify-write-append-event-sync sequence inside `with_bundle_lock`, a process-wide registry of one `Mutex` per canonicalized bundle path. Python's `ThreadingHTTPServer` has zero locking around this same read-modify-write cycle (a latent race there); a desktop UI issuing concurrent Tauri commands would turn it into a live one without this. Different bundles never block each other. | `v2/sopkb-rust/crates/sopkb-review/src/lock.rs::tests::{same_bundle_serializes_concurrent_writers,different_bundles_do_not_block_each_other}` | Phase 6 |
| P-R2 | An out-of-range-or-non-finite `confidence` edit is rejected for **`NaN` too**, not just values outside `[0, 1]`. Python's `NaN < 0`/`NaN > 1` are both false, so `NaN` passes bounds validation and gets written as the bare `NaN` JSON token -- invalid per the JSON spec, only re-readable by Python's own permissive `json.loads`. `serde_json::Value` has no representation for a non-finite JSON number, so reproducing "accepted, stored as NaN" would require bypassing this port's `Value`-based JSON pipeline for one field; rejected instead with the same `"confidence must be between 0 and 1"` message an out-of-range value gets. | `v2/sopkb-rust/crates/sopkb-review/src/review.rs::tests::edit_confidence_rejects_out_of_range_and_nan_and_garbage` | Phase 6 |
| P-R15 | Every review action now regenerates the four standard reports (`freshness.md`, `conflicts.md`, `extraction_summary.md`, `review_summary.md`) via `write_standard_reports`, in addition to the OKF sync Python already performs. In Python, only a full `validate_bundle` call refreshes these, so e.g. `review_summary.md` shows stale approve/reject counts immediately after approving an item until the next validate run -- a visible defect in a GUI that this port closes. `write_standard_reports` re-reads all its own state from disk, so this is a safe, side-effect-only addition with no other observable behavior change. | `v2/sopkb-rust/crates/sopkb-review/src/review.rs::tests::review_action_regenerates_standard_reports` | Phase 6 |
| P-M11 | **Superseded 2026-08-24, see below** -- `mine_with_author`'s LLM-authoring path originally traded Python's *partially destructive* failure mode (authored `.md` files from already-processed sections stay on disk while the four state JSON files never update after any section errors) for strict all-or-nothing atomicity: every section had to succeed, or nothing at all was written, even from sections that themselves succeeded. Real oss-launch has since replaced its own old partially-destructive behavior with graceful per-section degradation instead (see the new row below), which this port now matches; the all-or-nothing design is gone. | superseded by the test below | Phase 7, superseded 2026-08-24 |
| P-M11-2 | **Replaces P-M11.** `mine_with_author` now matches real oss-launch's own fix (found via the tools/sopkb staleness audit, `docs/port/CATCHUP_PLAN.md`'s 2026-08-24 section): a pre-validation pass (`validate_author_response`) drops individually malformed `documents`/`knowledge_items` entries instead of failing the whole response over one bad candidate; each section gets up to 3 attempts (retry on a transport error or on a response that still has validation errors, using whatever survives validation after the last attempt); a section that still fails after retries is skipped (not fatal) while every other section's output is still written. This closes a real bug a user hit in this exact worktree ("mining failed for 1 of 1 section(s), nothing written") that became much more likely once `sopkb_workbench::heading_restructure` started producing many more, smaller sections per document -- more sections means a higher chance at least one produces an unusable response, and P-M11's atomicity meant that one section could discard every other section's mined output. Disclosed deviation: oss-launch fans section `author()` calls out across a thread pool before applying sequentially; this port keeps sections fully sequential (see `mine_with_author`'s doc comment for why). | `crates/sopkb-mining/src/okf_author.rs::{mine_with_author_skips_failed_section_without_discarding_others, mine_with_author_retries_and_recovers_before_exhausting_attempts, validate_author_response_drops_malformed_knowledge_item_keeps_valid_ones, mine_with_author_apply_response_truthy_non_list_is_dropped_not_fatal_p_m19, mine_with_author_non_mapping_document_element_is_dropped_not_a_panic}` | Phase 7, added 2026-08-24 |
| P-M12 | `parse_author_response` tolerates a single Markdown code fence (` ```json ... ``` ` or ` ``` ... ``` `) wrapping the entire LLM response, stripping it before JSON parsing. Python's original does a strict `json.loads` with no fence stripping, so a model that wraps its answer in a fence (despite the system prompt instructing it not to) fails the whole run with `ValueError: LLM author response must be JSON: ...`; this is purely additive tolerance that only changes previously-failing runs into succeeding ones. | `crates/sopkb-mining/src/okf_author.rs::{strip_markdown_fence_removes_json_language_tagged_fence, strip_markdown_fence_removes_bare_fence, parse_author_response_tolerates_fence_p_m12}` | Phase 7 |
| P-M15 | `build_section_author_request`'s `source.resource` now reads the inventory record's `original_path` field, which actually exists, instead of Python's `source.get("path")`, a key inventory records never have (they carry `original_path`/`normalized_path`) -- so in the original, `resource` always silently fell back to `source_id`. This changes the outbound mining-request payload (and therefore the model's output) whenever a source's `original_path` differs from its `source_id`, which is true for essentially every real bundle (e.g. `sources/originals/glp1-intake-497fafac4587.md` vs. `glp1-intake-497fafac4587`). | `crates/sopkb-mining/src/okf_author.rs::build_section_author_request_p_m15_reads_original_path_not_path` | Phase 7 |
| P-M19 (raw-crash half) | `apply_author_response`'s falsy-coercion for `documents`/`knowledge_items` (`null`/`false`/`0`/`""`/`{}`/`[]` all become `[]`, only a truthy non-list raises) is preserved exactly. What changed: Python's `write_authored_document`/`authored_knowledge_item` call `.get(...)` directly on each `documents`/`knowledge_items` element, so a non-mapping element (e.g. a bare string in the list) raises a raw, uncaught `AttributeError`. This port checks `.as_object()` first and raises a typed `SopkbError::Value("authored document must be a mapping")` / `"authored knowledge item must be a mapping"` instead. Same treatment applied to `authored_knowledge_item`'s `metadata = dict(candidate.get("metadata") or {})`, which crashes on a truthy non-mapping `metadata` in Python (`dict("oops")`-style `ValueError`) -- converted to a typed error here too. | `crates/sopkb-mining/src/okf_author.rs::mine_with_author_non_mapping_document_element_is_typed_error_not_a_panic` (and `mine_with_author_apply_response_truthy_non_list_is_a_typed_error_p_m19`/`mine_with_author_apply_response_falsy_coercion_p_m19` for the preserved-coercion half) | Phase 7 |
| P-M24 | `concept_id_to_path` rejects any path component shaped like a Windows drive letter (`[A-Za-z]:...`), anywhere in the component sequence, not just a leading one. Python's `PurePosixPath("C:x").is_absolute()` is `False` (POSIX semantics only recognize a leading `/`), so such a component passes straight through validation unrejected -- inert on a POSIX host (no `C:` drive concept), but this port runs on Windows, where `okf_root.join(["C:", "evil.md"])`-style construction would let a crafted `concept_id` (e.g. from an LLM response) escape `okf_root` onto an arbitrary drive. All other traversal-guard behavior (`..` rejected only as a whole component, absolute-path rejection, empty-parts rejection, reserved-filename check at any depth) is preserved exactly. | `crates/sopkb-mining/src/okf_writer.rs::concept_id_to_path_p_m24_rejects_windows_drive_letter_traversal` | Phase 7 |
| P-M26 | `OkfDocument::validate` converts two latent non-`OKFDocumentError` Python crash paths into typed validation errors. (a) `frontmatter.sources: null` (or `false`/`0`, any non-iterable falsy value): Python's truthiness guard doesn't fire (falsy skips the type check), but the subsequent `for source in sources` then raises a raw `TypeError` iterating `None`/a bool/an int. This port treats every falsy `sources` value uniformly as "no sources" and skips validation, matching Python's evident intent without the crash (this also covers Python's non-crashing falsy cases, `""`/`{}`, identically -- both already iterate zero times in Python and still validate successfully here). (b) `frontmatter.sopkb` as a truthy non-mapping (e.g. `sopkb: "oops"`): Python's `(frontmatter.get("sopkb") or {}).get("rule")` calls `.get` directly on the truthy string/list/etc., raising a raw `AttributeError`. This port checks `.as_object()` first and raises `SopkbError::Value("frontmatter.sopkb must be a mapping")` instead. | `crates/sopkb-mining/src/okf_writer.rs::{validate_sources_null_is_treated_as_no_sources_not_a_crash, validate_sopkb_non_mapping_is_a_typed_error_not_a_crash}` | Phase 7 |
| P-P2 | `sopkb-mcp`'s stdio loop no longer terminates the whole process on a malformed-JSON or non-object stdin line. Python's original has the `json.loads` parse OUTSIDE the try/except that builds the JSON-RPC error envelope, so a single bad line crashes the entire server (stderr traceback, exit, no JSON-RPC response at all -- every pending/future request on that connection is lost). This port catches the parse/shape failure for THAT LINE ONLY, writes a `{"code": -32700, "message": "Parse error: ..."}` response with `id: null` (a request id can't be extracted from unparseable text), and continues the loop to the next line. | `v2/sopkb-rust/bin/sopkb-mcp/src/jsonrpc.rs::tests::{process_line_malformed_json_yields_parse_error_and_does_not_panic,process_line_non_object_json_yields_parse_error,end_to_end_jsonl_session_recovers_from_bad_line_and_keeps_processing}` | Phase 10 |
| P-P4 | `sopkb-mcp` now suppresses the JSON-RPC response entirely for any `notifications/*` method (e.g. `notifications/initialized`, which a spec-compliant MCP client sends immediately after `initialize`). Python's original has no notion of notifications and falls through to the generic `unknown JSON-RPC method` branch, returning an `error` response with `id: null` for every notification -- some spec-compliant clients treat an unsolicited error response with a null id as fatal and disconnect. This port writes no response line at all for a `notifications/*` method, matching what a real MCP client expects. | `v2/sopkb-rust/bin/sopkb-mcp/src/jsonrpc.rs::tests::{notifications_star_suppressed_entirely,end_to_end_jsonl_session_recovers_from_bad_line_and_keeps_processing}` | Phase 10 |
| P-P5 | `sopkb-mcp`'s JSON-RPC `error.message` text is now emitted unquoted uniformly for every error, instead of Python's exception-type-dependent `str(exc)`: `str(KeyError("x"))` wraps the message in single quotes (wire text `'unknown MCP tool: X'`), while `str(ValueError("x"))`/`str(PermissionError("x"))` have no quoting at all. Since this port's `SopkbError::Value` variant is used uniformly for every dispatch failure (there is no Rust equivalent of Python's exception-type-dependent `str()` behavior), every message is unquoted and consistent -- e.g. `unknown MCP tool: review.approve`, not `'unknown MCP tool: review.approve'`. A documented, deliberate simplification, not an oversight. | `v2/sopkb-rust/bin/sopkb-mcp/src/jsonrpc.rs::tests::unknown_method_error_unquoted_p_p5`; `v2/sopkb-rust/bin/sopkb-mcp/src/tools.rs::tests::dispatch_unknown_tool` | Phase 10 |
| P-P15 | `sopkb-mcp`'s stdout response lines are terminated with a raw `"\n"`, not `"\r\n"`. Python's text-mode `stdout` on Windows silently translates every `\n` written to `\r\n` unless the stream is explicitly reconfigured to binary/newline=""; this port's `writeln!` on a raw `Write` writes exactly one `\n` per line regardless of platform. Existing MCP clients tolerate either line ending, so this is expected to be harmless, but it is an observable byte-level difference in the wire output. | `v2/sopkb-rust/bin/sopkb-mcp/src/jsonrpc.rs::tests::end_to_end_jsonl_session_recovers_from_bad_line_and_keeps_processing` (asserts no `\r` appears anywhere in the captured output) | Phase 10 |
| P-P10 | `sopkb-mcp`'s `relations.search` tool no longer coerces a non-string `subject`/`predicate`/`object` argument to "no filter" (Python's `args.get(name) or ""`, which uses a truthy non-string value like a number/bool/non-empty array/object as-is, then crashes with a `TypeError` the moment it's `.lower()`-ed downstream). It now rejects any present, non-null, non-string value outright with `argument '<name>' must be a string`. Broadened slightly beyond the literal Python crash site: applied uniformly to every MCP tool argument whose own `inputSchema` already declares it `"string"` and optional -- also `review.note`'s `reviewer` field, which Python would have silently accepted as a non-string (no crash there, since Python never calls a string method on it, it's just stored verbatim in the review event) but this port now rejects the same way for internal consistency. `null`/absent still coerce to the schema-appropriate default (`""` for the three relation filters, `"mcp:user"` for `reviewer`), matching Python's falsy-value behavior exactly. | `v2/sopkb-rust/bin/sopkb-mcp/src/tools.rs::tests::dispatch_relations_search_rejects_non_string_filter_p_p10` | Phase 10 |
| P-P12 | `sopkb-mcp`'s `initialize` response no longer hardcodes `serverInfo.version` as the literal string `"0.1.0"`; it now reads `env!("CARGO_PKG_VERSION")` at compile time (the crate's actual `Cargo.toml` version, which currently also resolves to `"0.1.0"` -- no observable difference today, but it stops silently drifting from the crate's real version on a future bump). `protocolVersion: "2024-11-05-preview"` is unchanged (PRESERVE -- client-visible). | `v2/sopkb-rust/bin/sopkb-mcp/src/jsonrpc.rs::tests::initialize_response_shape_and_pkg_version_p_p12` | Phase 10 |
| P-W2 | **New generation counter + refuse-while-in-flight guard**, no Python analogue: `sopkb_workbench::WorkbenchHandle` refuses `set_workbench_root` (returns the new `SopkbError::Conflict`, leaving the context byte-for-byte unchanged) while any bundle-mutating operation (`run_ingest_pipeline_guarded`/`stage_uploaded_files_guarded`) is in flight for the CURRENT root, instead of the two documented alternatives being left unimplemented. Chosen over the "let it run and discard a stale-generation publish" alternative because it is strictly simpler to reason about and matches the state diagram's own `GuardInFlight -> Refused` shape directly. `generation` is still bumped on every committed switch. | `crates/sopkb-workbench/src/context.rs::tests::set_workbench_root_refused_while_mutation_in_flight_and_leaves_context_unchanged` (real concurrency test, same spirit as `sopkb-review/src/lock.rs`'s `same_bundle_serializes_concurrent_writers`); `set_workbench_root_bumps_generation_and_clears_selection` | Phase 9 |
| P-W3 | `list_bundles` (`sopkb_workbench::bundles::list_bundles`) no longer silently drops a bundle whose `bundle_describe`/`load_manifest` call fails (Python's `render_bundle_index` does `except Exception: continue`, making a broken project vanish from the index with zero explanation). Every directory that satisfies `is_bundle_dir` now produces a `BundleCard`; on a load failure, `summary`/`export_path` are `None` and `load_error` carries the error text instead of the whole entry disappearing. | `crates/sopkb-workbench/src/bundles.rs::tests::list_bundles_surfaces_load_error_instead_of_dropping_p_w3` | Phase 9 |
| P-W4 | `bundle_dir_for_key` (`sopkb_workbench::bundles::bundle_dir_for_key`) no longer treats a bundle's directory name and its `manifest["id"]` as equally-valid, collision-order-broken keys. The directory name is now canonical and always wins; a manifest-id match is consulted only as a secondary alias when no directory name matches, and can never override a real directory-name hit (previously, `dir.name == key OR manifest["id"] == key` could resolve ambiguously depending on `list_bundle_dirs`' sort order when two bundles' name/id sets collided). | `crates/sopkb-workbench/src/bundles.rs::tests::{bundle_dir_for_key_falls_back_to_manifest_id_alias_p_w4,bundle_dir_for_key_directory_name_wins_over_a_colliding_manifest_id}` | Phase 9 |
| P-W10 | Added `SopkbError::Conflict(String)` to `sopkb-core`'s error enum (minimal, additive -- no existing match site was exhaustive over `SopkbError`'s variants outside `error.rs`'s own `Display` impl, so nothing else needed updating). Used for exactly one new condition with no Python analogue: `set_workbench_root` refused because a mutation is in flight for the current root (see P-W2). Every other Phase 9 error path maps onto the four pre-existing variants (`NotFound` for unknown bundle/concept ids and missing source directories, `Value` for validation-shaped failures like a blank title or an unsafe upload path) -- `Conflict`/`MissingConfiguration` from PORT_PLAN.md's doc-E sketch did not both turn out to be needed; `MissingConfiguration`-shaped failures (e.g. mining with an unconfigured `azure-llm` profile) already surface as `SopkbError::Value` with Python's own "Missing ..." message text from `sopkb-config`/`sopkb-llm`, and reusing that preserves byte-for-byte message fidelity better than introducing a new variant would. | `crates/sopkb-workbench/src/context.rs::tests::set_workbench_root_refused_while_mutation_in_flight_and_leaves_context_unchanged` | Phase 9 |
| P-W16 | `sopkb_workbench::upload::stage_uploaded_files` takes a list of already-resolved source file **paths** (`UploadSource { path: PathBuf, relative_name: String }`), never raw bytes -- unlike Python's `UploadedSourceFile { filename, content: bytes }`, which exists only because HTTP multipart delivers bytes. There is no multipart-form parsing anywhere in this port; the function reads/copies the file itself. On-disk output (staged files, their content, and the traversal/sanitization rules) is unchanged -- this is an API-shape difference for a function with no direct Python caller-boundary equivalent, not a behavior change to anything observable through the filesystem. | `crates/sopkb-workbench/src/upload.rs::tests::stage_uploaded_files_preserves_relative_subpaths_on_disk` | Phase 9 |
| P-W19 | `sopkb_workbench::bundles::create_project` (and `init_bundle`) now return `already_existed: bool` alongside the bundle card/summary. Python's `handle_new_project_form` calls the already-idempotent `create_bundle`, which silently no-ops and returns the existing manifest untouched when `manifest.yaml` already exists -- yet the web UI's redirect always says "Project created" regardless, and the supplied title is silently ignored on the no-op path. This port's orchestration layer now surfaces that distinction so a future caller can say "opened existing project" instead of repeating the same lie. | `crates/sopkb-workbench/src/bundles.rs::tests::create_project_reports_already_existed_p_w19` | Phase 9 |
| P-W20 | `sopkb_workbench::bundles::create_project` and `init_bundle` now ALWAYS call `sync_okf_bundle` after `create_bundle` succeeds. Python's web path (`POST /new-project` -> `handle_new_project_form`) never did this (only the CLI `init` path did, via `cli.py`), so a web-created bundle previously started without `index.md`/`log.md`/its OKF index tree at all -- spec-invalid (A D8) even though `validate_bundle` never checked for it. Both of this port's bundle-creation entry points now converge on the CLI's (better) behavior. | `crates/sopkb-workbench/src/bundles.rs::tests::{create_project_always_syncs_okf_p_w20,init_bundle_always_syncs_okf}` | Phase 9 |
| P-W24 | `sopkb_workbench::concepts::get_concept_detail` and `focused_graph_for_concept` return a `NotFound` error for an unknown `concept_id`, instead of Python's `render_concepts`/`render_graph` silently falling back to the first entry (`web_app.py:910-913`, `:1238-1241`) when the query-string id doesn't match anything. This library layer never guesses on a caller's behalf; a future UI can still choose to fall back to `concept_index(...)[0]` itself if it wants that exact UX, but the choice is now explicit at the call site rather than baked in here. | `crates/sopkb-workbench/src/concepts.rs::tests::{get_concept_detail_unknown_id_is_not_found_p_w24,focused_graph_for_concept_unknown_id_is_not_found_p_w24}` | Phase 9 |
| P-W26 | `sopkb_workbench::upload::stage_uploaded_files` is the first call site in this port to construct paths under `.sopkb/uploads/current` from external (picker-supplied) input; `sopkb_core::store::state_path` itself performs no sanitization on its `filename` argument (by design -- it is also used for directory names like `"uploads"`/`"authored_okf"`). Every user-influenced path segment here is routed through `safe_upload_relative_path` (drops `.`/`..` components, sanitizes each remaining segment to `[A-Za-z0-9._ -]`) AND a belt-and-braces traversal check (P-W14 PRESERVE: the resolved destination must fall under the resolved staging directory, using a real filesystem resolve so a symlink planted inside a picked folder can't escape it either) before anything touches disk. | `crates/sopkb-workbench/src/upload.rs::tests::{safe_upload_relative_path_drops_dot_and_dotdot_segments,stage_uploaded_files_rejects_symlink_escape_p_w14}` (the symlink test is `#[cfg(unix)]` -- Windows symlink creation needs elevated privileges/developer mode and isn't exercised in this environment) | Phase 9 |
| P-W30 | `sopkb_workbench::context::build_context` returns a `WorkbenchContext { mode: Degraded, error: Some(..), .. }` instead of panicking or returning `Err` when `root` doesn't exist, isn't a directory, or `is_bundle_dir(root)` is true but `manifest.yaml` fails to load. Python has no equivalent failure surface at all: with no HTTP server to fail to start, "you picked a folder that doesn't work" would otherwise have no way to reach the user. `resolve_launch_target` (the very first, no-argument-yet launch) deliberately keeps Python's original silent-fallback-to-default-workbench-dir behavior instead -- only explicit switches via `build_context`/`set_workbench_root` surface `Degraded`. | `crates/sopkb-workbench/src/context.rs::tests::{build_context_degraded_for_nonexistent_root_p_w30,build_context_degraded_for_malformed_manifest_p_w30}` | Phase 9 |
| Q8 | `sopkb_workbench::upload::stage_uploaded_files` takes a `replace: bool` parameter (default-`true` call sites reproduce Python's `reset_upload_directory` wipe-on-every-submission via `stage_uploaded_files`'s own internal call to it; `replace: false` skips the wipe so files accumulate across calls -- "add more files" semantics a native multi-select picker invites, per docs/port/DECISIONS.md Q8). Folder picks are expected to bypass staging entirely at the caller level (`sopkb_workbench::ingest::IngestSource::Folder` points `scan_sources` straight at the picked directory); this crate's own `stage_uploaded_files` is never invoked for that path. | `crates/sopkb-workbench/src/upload.rs::tests::{stage_uploaded_files_replace_true_wipes_previous_set,stage_uploaded_files_replace_false_accumulates_q8}` | Phase 9 |
| P-R6 | `sopkb-cli`'s `review approve\|reject\|defer\|comment\|edit` no longer hardcodes an omitted `--reviewer` to the literal `"local:user"` (Python's `cli.py` `default="local:user"`, which never consults settings at all). It now resolves the same fallback chain the web path (`handle_review_form`) already used -- `sopkb_config::reviewer_name()`, and only `"local:user"` if that is *also* blank -- so the CLI and a future Tauri command agree on who "the reviewer" is by default. Per PORT_PLAN.md F4/P-R6: three reviewer-identity defaults existed (CLI `local:user`, web settings-aware, MCP's deliberate `mcp:user` audit marker); this converges the two *human* paths and leaves `mcp:user` untouched, as recommended. An explicitly passed `--reviewer <value>` is never overridden. | `v2/sopkb-rust/bin/sopkb-cli/src/lib.rs::tests::{reviewer_omitted_falls_back_to_configured_settings_name_p_r6,reviewer_omitted_and_unconfigured_falls_back_to_local_user_p_r6,explicit_reviewer_flag_is_never_overridden_p_r6}` | Phase 10 |
| W2-L1 | `sopkb_review::retire_source` is TRANSACTIONAL, where `source_lifecycle.py`'s original is not. Python writes `inventory.json`, `items.json`, `source_versions.json`, `source_events.json` and `manifest.yaml` one after another and only *then* calls `validate_bundle`, with no rollback: a failure part-way through -- or a validation pass that raises afterwards -- leaves the bundle permanently half-mutated. This port computes the complete new state in memory first (`sopkb_core::lifecycle::plan_retire_source`, pure: no I/O, no clock read) and writes it through a `FileTransaction` that captures each touched path's prior bytes; any error, including one from the post-write validation, restores every file. `reports/` is snapshotted up front because the validator rewrites it. Scope limit, documented at the type: the undo log is in memory, so this is atomic with respect to *errors*, not to a *crash* -- a killed process still leaves whatever subset it had written. An on-disk journal would be needed for that and is out of scope. | `crates/sopkb-review/src/source_lifecycle.rs::tests::{a_validation_failure_after_the_writes_rolls_every_file_back,the_rollback_test_is_not_vacuous_the_writes_do_land_before_validation_runs,a_bundle_recovers_completely_from_a_rolled_back_retire}` | Workstream 2 |
| W2-L2 | `sopkb_review::retire_source` is IDEMPOTENT. Python's original re-runs the whole mutation on an already-retired source and appends a second, near-identical `source_retired` event whose `previous_value.status` is `"retired"` -- permanently recording a state transition that never happened, in what is meant to be an append-only audit trail. This port returns `RetireOutcome::AlreadyRetired` before a transaction is even opened, so a repeated retire writes nothing at all: no duplicate event, no `manifest.updated_at` bump, and `sopkb_workbench::retire_bundle_source` additionally skips the OKF resync so the document tree is not rewritten either. | `crates/sopkb-review/src/source_lifecycle.rs::tests::retiring_twice_writes_nothing_the_second_time_and_never_duplicates_the_audit_event`, `crates/sopkb-core/src/lifecycle.rs::tests::plan_on_an_already_retired_source_is_a_no_op_returning_the_original_event`, `crates/sopkb-workbench/src/ingest.rs::tests::retire_bundle_source_retires_resyncs_and_is_inert_on_repeat` | Workstream 2 |
| W2-L3 | `sopkb_core::lifecycle::migrate_source_version_state` persists `setdefault`-only mutations. `bundle_store.py`'s original adds missing `supersedes`/`superseded_by` keys to items via `dict.setdefault` but never sets its `item_changed` flag when doing so, so an item missing ONLY those two keys was mutated in memory and the result silently discarded -- the write-back never fired, and every subsequent call redid the same no-op. Here every mutation flags the write-back. | `crates/sopkb-core/src/lifecycle.rs::tests::migration_persists_setdefault_only_additions_to_items` | Workstream 2 |
| W2-L4 | `sopkb_core::knowledge_lifecycle::merge_mined_items` supersedes pre-versioning items. `knowledge_lifecycle.py`'s original guards the supersede branch on `item_version` being truthy, so an item written before source versioning existed (`source_version_id` absent or null) stayed `lifecycle_status: "active"` forever while its replacement was appended alongside it -- the same claim counted twice, with no supersede edge between them. Closed structurally rather than with an extra branch: absent and empty version ids normalize to one "no version" sentinel which by construction is never a member of `current_versions` (that set only ever holds non-empty ids), so such an item takes the supersede branch. In a fully migrated bundle the case is unreachable anyway, since migration back-fills `source_version_id` on every item before mining runs; this is defence in depth for a bundle that reaches mining by a path that skipped migration. | `crates/sopkb-core/src/knowledge_lifecycle.rs::tests::a_pre_versioning_item_is_superseded_rather_than_left_active` | Workstream 2 |
| W2-L5 | `sopkb_core::inventory::scan_sources` no longer resets `sources/originals/`, and no longer drops inventory records whose input file has disappeared. Both follow from versioning rather than being independent choices: older versions' original bytes are referenced by the version registry and by every span offset recorded against them, so wiping the directory would invalidate the registry, and a source's history outliving its input file is the entire point of that registry. Removing a file from the scanned tree is therefore no longer a way to remove a source -- retirement is now an explicit, audited operation (`sources retire`). Matches current oss-launch behavior. | `crates/sopkb-core/src/inventory.rs::tests::{editing_a_source_adds_v2_and_supersedes_v1_keeping_both_originals,a_removed_input_file_leaves_its_source_record_in_place}` | Workstream 2 |
| W2-L6 | `sopkb_core::lifecycle::migrate_source_version_state` deliberately does NOT rename anything. A legacy source keeps its old content-hash-suffixed id (`intake-checklist-malformed01`) and its old unversioned `sources/normalized/<stem>.md` path, gaining only the version metadata that hangs off it -- because renaming would invalidate every section, knowledge, evidence, rule and relation id in the bundle, all of which embed the source id. This matches the reference implementation and is confirmed by the regenerated `malformed-null-confidence` fixture, whose migrated source still sits at its pre-versioning path while carrying a new `:v1` version entry. Consequence worth stating: a bundle ingested before this change keeps hash-suffixed ids forever unless re-ingested from source. | `crates/sopkb-core/src/lifecycle.rs::tests::migration_adds_source_version_defaults_to_existing_bundle_state` (asserts the normalized path is unchanged) | Workstream 2 |
| W2-L7 | `sopkb-cli` gains `sources retire` and `sources preview`, neither of which exists in `cli.py` (Python reaches retirement only through its web UI, and `classify_source_updates` only through the ingest form). Same category of deliberate addition as this port's existing `mine --profile` and `agent chat`. `--rationale` is REQUIRED on retire: a retirement with no stated reason is not worth having in an audit trail. | `v2/sopkb-rust/bin/sopkb-cli/src/cli.rs` (`SourcesCommand::{Retire,Preview}`), exercised via `crates/sopkb-workbench/src/ingest.rs::tests::{retire_bundle_source_retires_resyncs_and_is_inert_on_repeat,preview_reports_classifications_and_writes_nothing}` | Workstream 2 |
| G-A19-R | **`normalize_pdf` targets current `integration/oss-launch`, not `PORT_PLAN.md`'s pre-fork spec** (CATCHUP_PLAN.md decision D1), and the two disagree about output shape. `PORT_PLAN.md` and `port-mapping-a-core-data.md`'s `FUNCTION normalize_pdf` pseudocode both specify `"# Document\n\n"` followed by one `## Page N` **heading** per non-empty page. Current oss-launch emits `<!-- page N -->`, an **HTML comment**, and promotes the document's own first line into the H1 in place of the literal `Document`. The heading form made `extract_sections` split a spurious section at every page boundary (50 of 175 sections across 3 real PDFs were nothing else), which is why the Python changed; porting the older spec would deliberately reintroduce that. The G-A19 quirks themselves are PRESERVED unchanged: an empty page is skipped entirely with no OCR and no placeholder, and the number printed is the TRUE 1-based page index, so `<!-- page 1 -->` really is followed by `<!-- page 3 -->`. | `v2/sopkb-rust/fixtures/cases/{simple-pdf,multipage-gap-pdf,two-column-pdf}` via `crates/sopkb-core/tests/pdf_fixture_content.rs` (4 tests, byte-exact against `expected-python/` recorded from the real oss-launch CLI); `fixtures/harness/diff_normalize_pdf.py` (10 synthetic cases, byte-exact end-to-end vs. the real Python `normalize_pdf`) | Workstream 1 |
| G-A19-T | **`page.find_tables()` is not ported**, so `extract_pdf_page_text` is an exact port of oss-launch's `if not tables:` branch only. pdfplumber's table finder (ruling-line detection, edge merging, intersection/cell construction) is a substantial algorithm in its own right, and `_render_table_aware_column`, `_is_plausible_table` and the Y-band segmentation all hang off it. Output is therefore correct for any PDF with no detectable ruled table, and a PDF that does have one gets that region as ordinary flowed prose rather than a Markdown table -- the pre-fix behavior, not corrupted output. Marked by the `sopkb_core::pdf::layout::TABLES_NOT_PORTED` constant so the gap is greppable. Other deliberately-unported pieces of the same path, all of which lose text rather than corrupt it: composite (Type0/CID) fonts are handled for the common real-world shape only -- `Identity-H`/`Identity-V` 2-byte codes with `/W` widths (both the `c [w ...]` and `c_first c_last w` forms), `/DW`, and a `/ToUnicode` CMap, verified against pdfplumber by the `type0-identity-h` differential case; a Type0 font using a NON-identity predefined or embedded CMap still gets the 2-byte identity decode, which would mis-split codes for a mixed-byte-length CMap, Type3 glyph procedures are not executed, and clipping paths are ignored (pdfminer ignores them for text too). Image XObjects are skipped, which is not a deviation -- pdfminer skips them for text purposes as well, since there is no OCR. | `crates/sopkb-core/src/pdf/layout.rs::TABLES_NOT_PORTED` (the deferred branch is documented at the module head; no test asserts table rendering because none is produced) | Workstream 1 |
| G-A19-S | `is_non_latin_char_group` (oss-launch's `_is_non_latin_char_group`, which selects a page's `x_tolerance`) classifies scripts by Unicode **block ranges** rather than by `unicodedata.name(ch).startswith(("LATIN", "DIGIT"))`, because Rust's std carries no Unicode character-name database. The `"DIGIT"` half of the Python test is unreachable anyway -- it sits behind `ch.isalpha()`, and no digit is alphabetic. An alphabetic character whose Unicode name begins with `LATIN` but which falls outside the enumerated blocks would be scored non-Latin here; the only consequence is which `x_tolerance` the page gets (2 vs 3), and pages are scored by *fraction* against a 5% threshold, so isolated stragglers cannot flip a page's classification. | `crates/sopkb-core/src/pdf/layout.rs::tests::{non_latin_detection_matches_the_python_intent,x_tolerance_tightens_only_for_latin_majority_pages}` | Workstream 1 |
| G-A19-L | The `provider in {"azure-llm", "llm"}` per-page `restructure_headings_llm` fan-out inside `normalize_pdf` is deliberately **not** ported into `sopkb-core`, which has no LLM dependency (that lives in `sopkb-llm`, and `sopkb-core` must not depend on it). The fixture corpus runs the `fixture` provider, which skips that branch entirely, so no fixture-observable behavior differs. A future caller wanting LLM heading restructuring must apply it above this layer. | `crates/sopkb-core/src/pdf/mod.rs::normalize_pdf` (documented at the skipped branch) | Workstream 1 |
